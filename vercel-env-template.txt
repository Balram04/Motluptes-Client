# Vercel Environment Variables Template
# Add these in your Vercel project dashboard -> Settings -> Environment Variables

# Backend API URL (Replace with your actual Render URL)
REACT_APP_BASE_URL=https://your-app-name.onrender.com

# Admin Configuration
REACT_APP_ADMIN_EMAIL=admin@doghub.com

# Cloudinary (for image handling)
REACT_APP_CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name

# Build Configuration
GENERATE_SOURCEMAP=false

# Instructions:
# 1. Deploy your backend to Render first
# 2. Copy the Render URL (ends with .onrender.com)
# 3. Replace "your-app-name" in REACT_APP_BASE_URL with your actual Render service name
# 4. Go to Vercel dashboard -> Your Project -> Settings -> Environment Variables
# 5. Add each variable above with its actual value
# 6. Make sure to set environment variables for all environments (Production, Preview, Development)
# 7. Redeploy your frontend after adding variables